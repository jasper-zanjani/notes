#### ufw
:   
    Program for managing a [Netfilter](#netfilter) firewall.

    Allow traffic associated with various services
    ```sh
    ufw allow ssh
    ufw allow http
    ufw allow https
    ```