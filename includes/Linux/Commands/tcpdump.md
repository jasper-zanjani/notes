#### tcpdump
:   
    Inspect actual IP packets

    Display all network data
    ```
    tcpdump -i eth0   
    ```
    Set snapshot length of capture (default: 65,535B)
    ```sh
    tcpdump -s
    ```
